package com.autocardealer.inventory.domain;

/**
 * Enum representing dealer subscription tiers.
 * Used to categorize dealers and filter vehicles by subscription level.
 */
public enum SubscriptionType {
    BASIC,
    PREMIUM
}